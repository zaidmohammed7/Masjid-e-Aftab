import SettingsClient from "@/components/SettingsClient";

export const metadata = {
  title: "Settings | Masjid App",
  description: "Customize your app experience",
};

export default function SettingsPage() {
  return <SettingsClient />;
}
